# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*
brag-output/
uv.lock

# React SPA build output
taxmate/public/frontend/
taxmate/www/taxmate.html

# Agent / IDE tooling — local only, not shipped in production packages.
# Callers: git status / packaging (flit); no runtime importer.
# Existing purpose: this .gitignore already ignored .agents/.claude/.cursor/skills|agents;
# extend to full .cursor/, tasks/, skills-lock.json. User: "I want you to ensure that
# the next commit is about those files that are not requried in production - check
# the cursor rules file, and other files that shouldn't be in production - remove them"
.agents/
.claude/
.cursor/
skills-lock.json
tasks/
